# mixed-monorepo/AGENTS.md

## Scope

Example for .NET + Node + Python monorepo.

## Rules

- Demonstrate changed-project detection.
- Required-check aggregator always runs.
- Root config changes trigger all affected project types.
- Matrix output via JSON artifact, not workflow scalar outputs.
