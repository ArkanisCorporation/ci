# Composite actions

Shared step bundles used by reusable workflows.

Create `action.yml` in each subdirectory only when implementation starts. Keep each action narrow; prefer more small actions over one flag-heavy action.
