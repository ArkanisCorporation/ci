{
    "name": "typescript-pnpm-fixture",
    "private": true,
    "type": "module",
    "packageManager": "pnpm@10.0.0",
    "scripts": {
        "lint": "tsc -p tsconfig.json --noEmit",
        "test": "tsc -p tsconfig.json && node --test \"dist/**/*.test.js\"",
        "build": "tsc -p tsconfig.json"
    },
    "devDependencies": {
        "@types/node": "24.10.1",
        "typescript": "5.9.3"
    }
}
