name: GitHub Actions lint consumer fixture

on:
  pull_request:

permissions: {}

jobs:
  lint:
    name: workflows @ ${{ github.head_ref || github.ref_name }}
    uses: ArkanisCorporation/ci/.github/workflows/wf-lint-github-actions.yml@v1
    permissions:
      contents: read
    with:
      runs-on-json: '["ubuntu-latest"]'
      runs-on-self-hosted: false
      enable-cache: true
