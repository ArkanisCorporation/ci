name: Dotnet consumer fixture

on:
  pull_request:

permissions: {}

jobs:
  dotnet:
    name: .NET verification
    uses: ArkanisCorporation/ci/.github/workflows/wf-setup-dotnet.yml@v1
    permissions:
      contents: read
      pull-requests: write
    with:
      runs-on-json: '["ubuntu-latest"]'
      runs-on-self-hosted: false
      dotnet-version: 10.0.x
      solution: Example.slnx
      coverage: true
      coverage-report: true
      coverage-pr-comment: true
